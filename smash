import string
import random

# Function to check password strength
def check_password_strength(password):
    length = len(password)
    has_lower = any(ch.islower() for ch in password)
    has_upper = any(ch.isupper() for ch in password)
    has_digit = any(ch.isdigit() for ch in password)
    has_special = any(ch in string.punctuation for ch in password)

    score = sum([has_lower, has_upper, has_digit, has_special])

    # Classify password based on length and score
    if length < 6:
        return "Very Weak"
    elif length < 8:
        if score < 3:
            return "Weak"
        else:
            return "Moderate"
    else:
        if score == 4:
            return "Very Strong"
        elif score == 3:
            return "Strong"
        elif score == 2:
            return "Moderate"
        else:
            return "Weak"

# Function to suggest a strong password
def suggest_password():
    all_chars = string.ascii_letters + string.digits + string.punctuation
    # Generate a random strong password of 10-12 characters
    password = ''.join(random.choice(all_chars) for _ in range(random.randint(10, 12)))
    return password

# Main program
if __name__ == "__main__":
    print(" Strong Password Suggester Program ")
    user_pass = input("Enter your password: ")

    strength = check_password_strength(user_pass)
    print(f"\nPassword Strength: {strength}")

    if strength in ["Very Weak", "Weak", "Moderate"]:
        print(" Your password could be stronger.")
        print(" suggested Strong Password:", suggest_password())
    else:
        print(" Great! Your password is strong enough.")


string → gives predefined sets like letters, digits, and punctuation.

random → used to generate a random strong password.

It checks for:
•	Lowercase letters
•	Uppercase letters
•	Digits
•	Special characters
•	Length of the password
Then, it assigns a strength level using simple conditions:
It randomly combines:
•	Uppercase + lowercase letters
•	Digits
•	Special characters
to form a 10–12 character long strong password.

